/* Standalone Constant: It */

const It = {
                        ...getValueTransition(bt, "layout"),
                        onPlay: St,
                        onComplete: At
                    };

export default It;
