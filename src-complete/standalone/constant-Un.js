/* Standalone Constant: Un */

const Un = {
                                colorFormat: lt.RGBA8,
                                depthFormat: on,
                                scaleFactor: xt
                            };

export default Un;
