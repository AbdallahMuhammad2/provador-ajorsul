/* Standalone Function: D */

function D(d, o) {
    var c = o[of];
    c === void 0 && (c = o[of] = new Set);
    var h = d + "__bubble";
    c.has(h) || (pf(o, d, 2, !1),
    c.add(h))
}

export default D;
