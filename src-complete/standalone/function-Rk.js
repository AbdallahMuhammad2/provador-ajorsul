/* Standalone Function: Rk */

function Rk(d) {
    wk !== null && wk.tag === 0 && !(K & 6) && Hk();
    var o = K;
    K |= 1;
    var c = ok.transition
      , h = C;
    try {
        if (ok.transition = null,
        C = 1,
        d)
            return d()
    } finally {
        C = h,
        ok.transition = c,
        K = o,
        !(K & 6) && jg()
    }
}

export default Rk;
