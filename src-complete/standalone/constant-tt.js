/* Standalone Constant: tt */

const tt = {
                    metadata: {
                        version: 4.6,
                        type: "Curve",
                        generator: "Curve.toJSON"
                    }
                };

export default tt;
