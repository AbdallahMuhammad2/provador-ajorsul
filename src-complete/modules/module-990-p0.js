/*
 * Module 990 (Pattern 0)
 * Params: d
 * Size: 205 chars
 */

// === MODULE CONTENT ===
function module990(d) {
d.exports = function(o) {
            var c = document.createElement("style");
            return o.setAttributes(c, o.attributes),
            o.insert(c, o.options),
            c
        }
}

export default module990;
