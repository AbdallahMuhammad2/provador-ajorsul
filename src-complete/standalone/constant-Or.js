/* Standalone Constant: Or */

const Or = {
                size: 0,
                viewer: pr,
                array: Ir,
                offset: jr,
                width: ur.dataWindow.xMax - ur.dataWindow.xMin + 1,
                height: ur.dataWindow.yMax - ur.dataWindow.yMin + 1,
                channels: ur.channels.length,
                bytesPerLine: null,
                lines: null,
                inputSize: null,
                type: ur.channels[0].pixelType,
                uncompress: null,
                getter: null,
                format: null,
                colorSpace: three_module.Zr2
            };

export default Or;
