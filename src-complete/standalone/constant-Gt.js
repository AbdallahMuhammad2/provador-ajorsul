/* Standalone Constant: Gt */

const Gt = {
                buffer: At,
                byteOffset: Et,
                byteLength: index_modern_BufferUtils.concat(It).byteLength
            };

export default Gt;
