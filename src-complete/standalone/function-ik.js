/* Standalone Function: Ik */

function Ik(d, o) {
    var c = K;
    K |= 2;
    var h = Jk();
    (Q !== d || Z !== o) && (uk = null,
    Kk(d, o));
    do
        try {
            Tk();
            break
        } catch (_) {
            Mk(d, _)
        }
    while (!0);
    if ($g(),
    K = c,
    mk.current = h,
    Y !== null)
        throw Error(p(261));
    return Q = null,
    Z = 0,
    T
}

export default Ik;
