/* Standalone Function: Mg */

function Mg(d, o) {
    throw d = Object.prototype.toString.call(o),
    Error(p(31, d === "[object Object]" ? "object with keys {" + Object.keys(o).join(", ") + "}" : d))
}

export default Mg;
