/* Standalone Constant: eventListeners */

var eventListeners = {
    name: "eventListeners",
    enabled: !0,
    phase: "write",
    fn: function() {},
    effect,
    data: {}
};

export default eventListeners;
