/* Standalone Function: gi */

function gi(d, o, c, h) {
    if (50 < yk)
        throw yk = 0,
        zk = null,
        Error(p(185));
    Ac(d, c, h),
    (!(K & 2) || d !== Q) && (d === Q && (!(K & 2) && (qk |= c),
    T === 4 && Ck(d, Z)),
    Dk(d, h),
    c === 1 && K === 0 && !(o.mode & 1) && (Gj = B() + 500,
    fg && jg()))
}

export default gi;
