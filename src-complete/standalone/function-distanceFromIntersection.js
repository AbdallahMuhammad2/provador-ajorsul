/* Standalone Function: distanceFromIntersection */

function distanceFromIntersection(d, o, c) {
    c.vsub(d, v0);
    const h = v0.dot(o);
    return o.scale(h, intersect),
    intersect.vadd(d, intersect),
    c.distanceTo(intersect)
}

export default distanceFromIntersection;
