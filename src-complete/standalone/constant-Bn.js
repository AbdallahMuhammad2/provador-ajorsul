/* Standalone Constant: Bn */

const Bn = {}
                              , cn = _n.attributes;
                            let Yr = 0;
                            const Jr = dn.getAttributes();
                            for (const sn in Jr)
                                if (Jr[sn].location >= 0) {
                                    let on = cn[sn];
                                    on === void 0 && (sn === "instanceMatrix" && Hr.instanceMatrix && (on = Hr.instanceMatrix),
                                    sn === "instanceColor" && Hr.instanceColor && (on = Hr.instanceColor));
                                    const Un = {};

export default Bn;
